framework:
    validation:
        email_validation_mode: html5

        # Enables validator auto-mapping support.
        # For instance, basic Required/Blank/NotNull constraints will be inferred from Doctrine's ORM mapping.
        #auto_mapping:
        #    App\Entity\: []
